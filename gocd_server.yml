---
- name: ping to gocd server

  hosts:
    - gocd_server

  become: yes

  tasks:
    - name: download {{ pkg_path }}
      copy: src={{ pkg_path }} dest=/tmp/{{ pkg_path }}

    - name: install {{ pkg_path }}
      apt: deb=/tmp/{{ pkg_path }}

